using System.Windows.Input;

namespace JRAltdIncProgramUpdater.Services;

/// <summary>
/// Minimal ICommand implementation for wiring a Button inside a DataTemplate
/// (e.g. the per-card Skip button) back to a handler on the hosting window,
/// via Command/CommandParameter binding rather than routed-event bubbling.
/// </summary>
public sealed class RelayCommand : ICommand
{
    private readonly Action<object?> _execute;
    private readonly Predicate<object?>? _canExecute;

    public RelayCommand(Action<object?> execute, Predicate<object?>? canExecute = null)
    {
        _execute = execute;
        _canExecute = canExecute;
    }

    public bool CanExecute(object? parameter) => _canExecute?.Invoke(parameter) ?? true;

    public void Execute(object? parameter) => _execute(parameter);

    public event EventHandler? CanExecuteChanged
    {
        add => CommandManager.RequerySuggested += value;
        remove => CommandManager.RequerySuggested -= value;
    }
}
